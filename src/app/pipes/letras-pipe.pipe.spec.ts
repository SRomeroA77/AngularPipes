import { LetrasPipePipe } from './letras-pipe.pipe';

describe('LetrasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LetrasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
